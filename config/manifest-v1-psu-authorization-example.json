{
  "schemaVersion": "v1",
  "name": "FAPI 1 Advanced hybrid PSU authorisation (runtime-configured manual example)",
  "certificationCoverage": "partial",
  "steps": [
    {
      "id": "discovery",
      "name": "OpenID discovery document",
      "request": {
        "method": "GET",
        "url": "${config.discoveryUrl}"
      },
      "assertions": [
        { "type": "http_status", "expected": 200 },
        { "type": "json_field", "path": "authorization_endpoint", "rule": "https_url" },
        { "type": "json_field", "path": "token_endpoint", "rule": "https_url" }
      ]
    },
    {
      "kind": "psu-authorization",
      "id": "psu-authorization",
      "name": "PSU authorisation (manual)",
      "mode": "manual",
      "authorizationEndpoint": "${steps.discovery.response.body.authorization_endpoint}",
      "clientId": "${config.oauth.clientId}",
      "redirectUri": "${config.oauth.redirectUri}",
      "responseType": "code id_token",
      "scope": "openid accounts",
      "requestObject": {
        "source": "fapi-signing",
        "audience": "${steps.discovery.response.body.issuer}"
      },
      "timeoutSeconds": 180,
      "mandatory": true
    },
    {
      "id": "token-exchange",
      "name": "Token exchange (authorization_code)",
      "request": {
        "method": "POST",
        "url": "${steps.discovery.response.body.token_endpoint}",
        "body": {
          "encoding": "form",
          "fields": {
            "grant_type": "authorization_code",
            "code": "${steps.psu-authorization.response.body.code}",
            "client_id": "${config.oauth.clientId}",
            "redirect_uri": "${config.oauth.redirectUri}"
          }
        }
      },
      "tokenEndpointAuthPolicy": {
        "source": "fapi-signing"
      },
      "assertions": [
        { "type": "http_status", "expected": 200 },
        { "type": "json_field", "path": "access_token", "rule": "required" }
      ]
    }
  ]
}
