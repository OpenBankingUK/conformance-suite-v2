"""REST API for the local conformance suite runtime.

Provides endpoints for starting conformance runs, polling their status,
and retrieving structured results — enabling headless/CI usage without
the CLI.
"""
